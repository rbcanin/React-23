import { useState } from "react";

const HookUseState = () => {
  //hooks renderizam valores
  //UseState

  let userName = "Rebeca";

  return (
    <div>
      <h2>UseState</h2>
      <p>Variável: {userName}</p>
      <p>useState: {name}</p>
      <hr />
    </div>
  );
};

export default HookUseState;
